{
  "name": "reclaim-openwith-extension",
  "version": "1.0.0",
  "description": "Chrome extension for Reclaim: Open With native application launching",
  "type": "module",
  "scripts": {
    "build": "node esbuild.config.js",
    "watch": "node esbuild.config.js --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "@types/jsdom": "^27.0.0",
    "esbuild": "^0.20.0",
    "jsdom": "^27.2.0",
    "typescript": "^5.3.3",
    "vitest": "^2.1.0"
  },
  "dependencies": {
    "buffer": "^6.0.3",
    "docx": "^9.5.1",
    "xlsx": "^0.18.5"
  }
}
